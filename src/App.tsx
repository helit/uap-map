import "./App.css";

import MapContainer from "./components/MapContainer";

const App = () => {
  return (
    <div style={{ display: "flex" }}>
      <MapContainer />
    </div>
  );
};

export default App;
